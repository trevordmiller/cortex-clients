{
  "name": "cortex-clients-shared",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "babel src -d lib",
    "dev": "yarn build -- -w",
    "prepublish": "yarn build"
  },
  "devDependencies": {
    "babel-cli": "^6.24.1",
    "babel-preset-env": "^1.4.0",
    "babel-preset-react": "^6.24.1"
  },
  "dependencies": {
    "react": "^15.5.4",
    "react-apollo": "^1.0.1",
    "react-native-web": "^0.0.81"
  },
  "main": "lib/index.js",
  "files": [
    "package.json",
    "README.md",
    "lib"
  ],
  "babel": {
    "presets": [
      "env",
      "react"
    ]
  }
}
